version: v1.0
name: Update & Upgrade System

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004

blocks:
  - name: Run Update Script
    task:
      jobs:
        - name: Update System
          commands:
            - checkout
            - chmod +x update_upgrade.sh
            - ./update_upgrade.sh
